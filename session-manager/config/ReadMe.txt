firRDS Sessionmanager DBUS configuration:

to allow the session manager to use the dbus for session change notification,
put the config file ogon.SessionManager.conf in the system.d directory
of our local dbus installation.

On Ubuntu 13.10 the path is /etc/dbus-1/system.d/